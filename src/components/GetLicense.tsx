import React from "react";

// No-op GetLicense component — removed checkout flow for free build
export const GetLicense = ({
  
}: {
  setState?: React.Dispatch<React.SetStateAction<boolean>>;
  buttonText?: string;
  buttonClassName?: string;
}) => {
  return null;
};
